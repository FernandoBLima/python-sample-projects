from setuptools import setup

setup(
    name='producer',
    version='1.0',
    description='Server used to create messages for rabbitmq',
    author='Fernando',
    author_email='@',
    packages=['producer'],
)
